server {
    listen 80;
    server_name teahauth.eu api.teahauth.eu;
    
    # HTTP to HTTPS redirect
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name teahauth.eu;

    # Kendi klasörüne bakıyor
    ssl_certificate /etc/nginx/certs/techauth.eu/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/techauth.eu/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl;
    server_name api.teahauth.eu;

    # Eğer tek sertifikan varsa şimdilik techauth.eu klasörüne bakabilir.
    # Ayrı aldığında burayı api.teahauth.eu olarak güncellersin.
    ssl_certificate /etc/nginx/certs/techauth.eu/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/techauth.eu/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
